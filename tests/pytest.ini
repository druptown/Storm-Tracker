[pytest]
# Dwingt rootdir=tests/ af, ongeacht vanuit welke directory pytest wordt
# aangeroepen. Zonder dit bestand kan pytest de bovenliggende map
# (storm_tracker_v3/, die __init__.py bevat en dus een echte Home
# Assistant-installatie vereist) als package proberen te importeren
# tijdens collectie, wat altijd faalt in een test-/CI-omgeving.
testpaths = .
